/* Open when someone clicks on the span element */
function openNav() {
    document.getElementById("skyLaunchNav").style.height = "100%";
}

/* Close when someone clicks on the "\" symbol inside the overlay */
function closeNav() {
    document.getElementById("skyLaunchNav").style.height = "0%";
}